#include <bits/stdc++.h>
using namespace std ; 
int64_t a, b; 
#define bigint __int128_t
int32_t main(int32_t argc,char*argv[])
{
    cin.tie(0)->sync_with_stdio(false);
    cerr.tie(0) ;
    cin >> a >>  b;
    return cout << (int64_t)(a + b) <<"\n" , 0 ; 
}